# Linux integration

This directory will hold board-specific Linux integration files:

```text
dts/     device tree sources
config/  kernel defconfig or config fragments
patches/ optional kernel patches if needed
```

The first target is a minimal serial-console initramfs boot.
